name: Images / Radicale

on:
  push:
    branches:
      - master
    paths:
      - images/radicale/**
      - Makefile

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4
      - run: make images/radicale -B
